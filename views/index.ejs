<!DOCTYPE html>
<html lang="en">
<%- include ('partials/head.ejs'); -%>
<body>
    <%- include ('partials/nav.ejs'); -%>
    <div class = "exercise content">
    <h2>All Users</h2>

    <% if (users.length > 0) {  %> 
        <% users.forEach(user =>  {  %>
            <a class = "single" href ="/api/exercise/log/<%= user._id %>">
            <h3 class = "title"> <%= user.username %> </h3>
            <% }) %>
            </a>
        <% } else { %>
            <p>there are no users to display...</p>
            <% } %> 

        </div>
</body>

</html>